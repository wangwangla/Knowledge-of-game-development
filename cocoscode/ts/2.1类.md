# 类

```java
function person(){
    this.name= "";
}
new Perdon ();
```

###  继承

```java
function web(){
    Person.call(this);
}

web wv= new web();
可以继承构造函数的中方法，不可以继承原型类的中的方法
 
    
    
--------------
原型链继承
Web.prototype = new Person(); 
原型链 的是 没有办法 给父类传参数的
```



###  ts类

```java
class Person{
    name : String;
    constructor(n:String){
        this.name = n;
    }
    
   //method
    run():void{
        alert(this.name);
    }
    
    gettName():String{
        return this.name;
    }
    
    setName(name:String){
        this.name = name;
    }
}


var p = new Person("张三");
p.getName()
```

